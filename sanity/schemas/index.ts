import user from './user-schema';
import post from './post-schema';
import postBy from './postBy-schema';
import comment from "./comment-schema";

const schemas = [user, post,postBy,comment];

export default schemas